[package]
name = "plugin_sdk"
version = "0.1.0"
edition = "2024"

[lib]
name = "plugin_sdk"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
rmp-serde = "1"
rmpv = { version = "1", features = ["with-serde"] }
